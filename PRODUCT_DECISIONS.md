# Unvirtual Display 제품 결정 문서

> 상태: v1 배포 기획 초안 0.7
> 목적: Windows와 macOS에 설치 가능한 첫 정식 배포본의 제품 방향, 출시 범위, 기술 선택과 미결정 사항을 한곳에서 관리한다.

## 1. 제품 한 줄 정의

**Unvirtual Display는 사용자가 가진 3D 모델과 2D 이미지를 데스크톱 위의 작은 가상 진열장에 배치하고 감상하는 Windows/macOS 앱이다.**

단순한 모델 뷰어나 바탕화면 장식이 아니라, 디지털 소장품을 직접 진열하고 계속 곁에 두는 경험을 목표로 한다.

### 핵심 가치

1. 파일을 넣는 즉시 보기 좋은 전시물이 된다.
2. 진열장을 꾸미고 배치하는 과정 자체가 즐겁다.
3. 평상시에는 데스크톱 작업을 방해하지 않는다.
4. 사용자의 소장품은 기본적으로 기기 안에 안전하게 보관된다.

### 초기 타깃 사용자

- 3D 피규어, 캐릭터 모델, 팬아트를 수집하는 사용자
- 바탕화면 꾸미기와 데스크톱 위젯을 좋아하는 사용자
- 자신이 만든 3D 모델이나 일러스트를 상시 전시하고 싶은 창작자

## 2. 가장 먼저 확정할 제품 결정

아래 항목은 기술 구조와 v1 출시 범위를 크게 바꾸므로 구현 전에 우선 결정한다.

| 우선순위 | 결정 사항 | 선택지 | 현재 추천안 | 상태 |
|---|---|---|---|---|
| P0 | 첫 출시 OS | Windows / macOS / 동시 지원 | Windows와 macOS 동시 배포 | 확정 |
| P0 | 화면 위 동작 방식 | 일반 창 / 항상 위 / 바탕화면 레이어 | 일반 창이 기본, 항상 위와 클릭 통과는 사용자 옵션 | 확정 |
| P0 | 첫 버전의 콘텐츠 | 3D만 / 2D만 / 둘 다 | 사용자 소유 3D 모델과 2D 이미지를 모두 지원 | 확정 |
| P0 | 렌더링 기술 | Three.js / Babylon.js / 네이티브 엔진 | Three.js | 확정 |
| P0 | 데스크톱 프레임워크 | Tauri / Electron / 네이티브 | Electron + React + TypeScript | 확정 |
| P0 | 콘텐츠 저장 방식 | 원본 참조 / 앱 내부 복사 / 선택형 | 프로젝트에 복사하는 방식을 기본값으로 사용 | 확정 |
| P1 | 배포 모델 | 무료 / 원하는 만큼 지불 / 유료 | 무료 다운로드 + 선택적 후원 | 확정 |
| P1 | 첫 배포처 | itch.io / 자체 웹사이트 / 앱 스토어 | itch.io | 확정 |
| P1 | 공유 기능 | 이미지 / 개인 백업 / 에셋 포함 공유 | 이미지 캡처와 개인 백업용 `.uvd`; 사용자 에셋의 공개 공유는 지원하지 않음 | 확정 |

## 3. 핵심 사용 흐름

첫 실행부터 감상까지의 이상적인 흐름은 다음과 같다.

1. 사용자가 기본 진열장 템플릿을 선택한다.
2. 3D 모델 또는 이미지를 창에 드래그한다.
3. 앱이 크기, 중심점, 바닥 위치를 자동으로 보정해 진열한다.
4. 사용자가 이동, 회전, 크기와 조명을 간단히 조정한다.
5. 편집 모드를 종료하면 조작 UI가 사라지고 클릭 통과가 활성화된다.
6. 앱을 다시 실행해도 진열 상태와 창 위치가 복원된다.

### 반드시 매끄러워야 하는 순간

- 파일 드롭 후 첫 결과가 바로 보기 좋아야 한다.
- 편집 모드와 감상 모드의 차이가 명확해야 한다.
- 진열장이 다른 앱의 클릭과 작업을 방해하지 않아야 한다.
- 예상치 못한 종료 뒤에도 배치 상태가 보존되어야 한다.

## 4. v1 정식 배포 범위

v1은 기능 시연용 빌드가 아니라, 일반 사용자가 설치하고 계속 사용할 수 있는 완성된 데스크톱 제품을 목표로 한다. 개발 과정의 기술 검증 빌드는 내부에서만 사용하고, 외부에 내놓는 첫 빌드는 아래 조건을 만족한 정식 배포본으로 만든다.

### v1 사용자 기능

- 투명 배경의 크기 조절 가능한 데스크톱 창
- 편집 모드와 감상 모드
- 창 항상 위 옵션
- 감상 모드의 클릭 통과 옵션
- 다중 모니터 위치 기억과 디스플레이 변경 시 안전한 위치 복원
- 교체 가능한 진열장 프리셋: 미니멀 갤러리, 유리 진열장, 따뜻한 미니어처 선반 등
- 진열장 종류를 바꿔도 배치한 전시물을 최대한 유지하는 템플릿 구조
- 진열장 크기, 선반 수, 프레임과 바닥 재질 설정
- 3D 파일 가져오기: `.glb`, `.gltf`, `.vrm`, `.fbx`, `.obj`
- 2D 파일 가져오기: `.png`, `.jpg`, `.jpeg`, `.webp`
- 2D 전시 타입: 평면 패널, 아크릴 스탠드, 액자, 포토 카드
- 모든 2D 콘텐츠를 두께, 재질, 받침대와 그림자가 있는 3D 전시물로 표현
- 오브젝트 선택, 이동, 회전, 크기 조절, 삭제
- 선반 및 바닥 스냅, 복제, 잠금, 이름 변경
- 정해진 칸에 묶이지 않는 자유 배치와 오브젝트 간 겹침 허용
- 단순화된 충돌체와 기본 중력을 이용한 낙하, 충돌, 쌓기와 안착
- 전시물별 `넘어짐 방지`와 충돌 무시 설정
- 실행 취소와 다시 실행
- 카메라 시점과 줌 조절
- 조명 밝기, 색상, 방향과 품질 프리셋
- 모델별 애니메이션 켜기/끄기, 클립 선택, 반복과 속도 설정
- 애니메이션 기본값은 정지이며 사용자가 켠 모델만 재생
- 여러 진열장 프로젝트 생성, 이름 변경, 복제와 삭제
- 프로젝트 자동 저장, 백업과 비정상 종료 복구
- 개인 백업 및 기기 이동을 위한 `.uvd` 프로젝트 내보내기와 가져오기
- 현재 진열장 투명 또는 배경 포함 PNG 캡처
- 첫 실행 온보딩, 설정 화면과 단축키 안내
- 한국어, 영어, 일본어와 중국어 간체 UI
- 앱 내 오류 안내 및 진단 로그 내보내기
- 로컬 파일 기반 동작

### v1 배포 완성 조건

- Windows와 macOS용 설치 파일을 제공한다.
- 앱 이름, 아이콘, 버전, 저작권과 라이선스 정보를 갖춘다.
- 유료 코드 서명과 macOS 공증 없이 배포하며 OS 보안 경고와 수동 실행 방법을 itch.io 페이지에 명확히 안내한다.
- itch 앱 설치자는 패치 업데이트를 사용하고 웹 직접 설치자는 새 버전을 수동으로 다시 받는다.
- 새 설치, 업데이트, 제거를 실제 기기에서 검증한다.
- 일반 권한 사용자 계정에서 정상 실행된다.
- 절전 복귀, 모니터 연결 변경, 해상도와 배율 변경을 처리한다.
- 지원하지 않는 파일과 너무 큰 파일을 불러올 때 앱이 종료되지 않고 복구 가능한 오류를 보여준다.
- 자동 저장 중 종료되어도 마지막 정상 프로젝트를 복원할 수 있다.
- 유휴 상태의 CPU/GPU 사용량과 메모리 사용량이 정한 기준을 넘지 않는다.
- 개인정보 처리방침, 오픈소스 고지와 최소 사용 설명서를 제공한다.
- itch.io 프로젝트 페이지, Windows/macOS 다운로드 채널과 릴리스 노트를 제공한다.
- 치명적 오류 없이 대표 사용 시나리오를 통과하는 릴리스 체크리스트를 운영한다.

### v1 이후 확장 후보

- 커뮤니티 및 공개 갤러리
- 자체 진열장 테마와 장식 확장
- 여러 사용자의 실시간 공동 편집
- 메시 단위 정밀 충돌, 관절·천·머리카락과 래그돌 같은 고급 물리 시뮬레이션
- 고급 포즈와 애니메이션 편집기
- Blender, Unity와 MMD 계열 포맷을 위한 변환 도구 또는 가이드
- Steam, Spotify 등의 외부 서비스 연동
- 모바일 버전

이 항목들은 제품 완성도에 필요해서 빠진 것이 아니라 별도의 서버, 운영 정책 또는 전문 편집 기능이 필요한 확장 영역이다. v1은 로컬 데스크톱 진열장 자체를 완제품으로 만드는 데 집중한다.

### 의도적으로 지원하지 않는 기능

- 사용자 계정, 로그인과 클라우드 동기화
- 앱 내부 모델 마켓플레이스
- VRoid Hub, BOOTH, Sketchfab 등 외부 모델 사이트의 계정 연동
- 외부 모델 검색, 미리보기, 구매와 다운로드
- 제3자 모델을 앱 설치 파일에 기본 샘플로 포함하는 기능
- 사용자 모델을 서버에 업로드하거나 공개 배포하는 기능

모델을 구하는 일은 앱 밖에서 이루어진다. 향후 공식 웹사이트나 사용 가이드에서 외부 사이트를 단순히 소개할 수는 있지만, 앱 자체는 사용자가 가진 로컬 파일을 불러오고 진열하는 역할에 집중한다.

## 5. UX 및 시각 디자인 결정 사항

### 5.1 창의 성격

전시 창과 편집 창을 분리한다.

- 전시 창은 테두리 없는 투명 위젯이며 실제 Three.js 진열 장면만 보여준다.
- 편집 창은 일반 데스크톱 창이며 프로젝트, 전시물 목록, 속성, 진열장과 조명 설정을 제공한다.
- 편집 중에는 전시 창이 마우스 입력과 3D 조작을 받고, 감상 중에는 클릭 통과 상태가 된다.
- 투명 모드에서는 실제로 그려진 픽셀만 입력을 받고 빈 투명 영역은 자동으로 바탕화면에 클릭을 통과시킨다.
- 전시 창을 더블클릭하거나 편집기의 조정 버튼을 누르면 이동 바, 테두리와 8방향 크기 조절 손잡이가 나타난다.
- 전시 창 배경은 투명, 단색 또는 로컬 이미지로 선택하며 이미지 모드에서 파일을 지정하지 않으면 검은색을 사용한다.
- 여러 진열장 프로젝트를 저장할 수 있지만 화면에는 사용자가 활성화한 한 개만 표시한다.
- 전시 창은 일반 창 상태를 기본으로 한다.
- `항상 위`와 `클릭 통과`는 서로 독립된 옵션으로 둔다.
- 트레이 메뉴 또는 전역 단축키로 편집 창 열기와 감상 모드 전환을 제공한다.
- 진짜 바탕화면 레이어 배치는 OS별 차이가 크므로 v1 필수 기능으로 두지 않는다.

### 5.2 편집 방식

편집기는 진열장 템플릿의 정해진 슬롯이나 격자에 전시물을 강제로 끼우지 않는다. 사용자는 피규어를 겹치거나 다른 피규어 위에 쌓는 등 3D 공간을 자유롭게 사용할 수 있다.

- 기본 조작은 드래그, 회전과 휠로 단순화한다.
- 정밀 조작은 작은 속성 패널에서 숫자로도 가능하게 한다.
- 이동·회전·크기 조절용 3D 기즈모를 필요할 때 표시한다.
- 바닥, 선반 또는 다른 오브젝트 표면에 붙는 스냅 기능을 제공한다.
- 실수로 배치를 잃지 않도록 Undo/Redo를 v1 필수 기능으로 포함한다.

#### 충돌과 쌓기

- 임포트할 때 모델의 바운딩 박스 또는 단순화된 형태로 충돌체를 자동 생성한다.
- 선반, 바닥과 진열장 벽에도 충돌체를 둔다.
- 기본 중력을 적용해 공중에서 놓은 전시물이 바닥, 선반 또는 다른 전시물 위에 떨어져 멈추게 한다.
- 드래그 중에는 전시물이 포인터를 직접 따라가도록 하고, 놓는 순간부터 충돌과 중력을 다시 적용한다.
- 드래그 중 겹침은 외곽선이나 색상으로 알려주고, 놓은 뒤에는 물리가 겹침을 해소한다.
- `표면에 놓기`는 중력이 작용하기 전에 원하는 위치를 빠르게 잡아주는 편의 기능으로 유지한다.
- 오브젝트별 충돌 켜기/끄기 또는 임시 충돌 무시 조작을 제공해 의도적인 겹침을 허용한다.
- 움직이는 물체가 모두 안정되면 물리 바디를 자동 휴면시키고, 편집이나 충돌이 생기면 다시 깨운다.
- 정밀한 삼각형 메시 충돌보다 안정적인 박스, 캡슐 또는 단순 볼록 충돌체를 우선한다.

#### 넘어짐 방지와 배치 잠금

`포즈 고정`이라는 이름은 캐릭터의 뼈대 포즈 또는 애니메이션 고정과 혼동되므로 물리 옵션의 UI 명칭은 **`넘어짐 방지`**로 사용한다.

- `넘어짐 방지`를 켜면 중력과 위치 충돌은 그대로 적용하되 물리로 인한 회전을 막아 사용자가 정한 자세를 유지한다.
- 사용자가 편집 도구로 회전하면 그 방향을 새로운 기준 자세로 저장한다.
- 가져온 3D 피규어와 세워 두는 2D 전시물은 `넘어짐 방지`를 기본으로 켠다.
- 옵션을 끄면 완전한 동적 물체처럼 기울거나 넘어지고 구를 수 있다.
- 기존 `배치 잠금`은 위치와 회전을 모두 고정하고 편집도 막는 별도 기능으로 유지한다.

이 방식은 기본적인 물리 감각과 자연스러운 쌓기를 제공하면서, 캐릭터 피규어가 작은 충격에도 계속 쓰러지는 문제를 피한다. 의도적인 겹침은 충돌 무시로, 완전히 고정된 전시는 배치 잠금으로 처리한다.

### 5.3 진열장 표현

기본 미술 방향은 **미니멀 갤러리에 따뜻한 미니어처 감성을 소량 섞은 고급 굿즈 진열장**으로 확정한다. 다양한 스타일의 사용자 모델이 주인공이 되도록 진열장과 UI는 강한 캐릭터성을 피한다.

- 중립적인 색상, 절제된 형태와 따뜻한 스포트라이트를 기본으로 한다.
- 반투명 유리, 아크릴, 밝거나 짙은 목재와 무광 금속을 주요 재질로 사용한다.
- 사실적인 재질감을 사용하되 과도한 반사와 굴절로 전시물을 가리지 않는다.
- 반투명 유리, 미니멀 프레임, 미니어처 선반 등 여러 진열장 프리셋을 제공한다.
- 진열장 프리셋은 외형과 기본 조명만 바꾸고 전시물 배치는 가능한 한 유지한다.
- 재질의 정교함보다 다양한 기기에서 안정적인 투명도와 그림자를 우선한다.
- 진열장 크기에 맞춰 선반이 자동으로 조절되도록 한다.

### 5.4 접근성과 편의성

- UI 확대 비율과 고해상도 디스플레이 대응
- 키보드만으로 주요 동작 수행 가능 여부
- 조작 단축키 표시
- 낮은 성능의 GPU를 위한 품질 옵션
- 전체 애니메이션 끄기와 모델별 애니메이션 재생 여부 설정

## 6. 콘텐츠 처리 결정 사항

### 6.1 3D 콘텐츠

v1은 `GLB`, `GLTF`, `VRM`, `FBX`, `OBJ`를 직접 지원한다. GLB와 VRM은 에셋이 하나의 파일에 들어 있어 가장 안정적인 권장 포맷으로 안내한다. GLTF, FBX와 OBJ는 외부 텍스처 및 재질 파일을 함께 선택하거나 폴더째 드롭할 수 있게 한다.

포맷별 역할:

- `.glb`: 일반 3D 모델의 기본 권장 포맷
- `.gltf`: 외부 바이너리와 텍스처가 분리된 glTF 프로젝트
- `.vrm`: VRoid 및 일본 캐릭터형 휴머노이드 모델; VRM 0.x와 1.0 지원
- `.fbx`: 일반 모델 마켓과 캐릭터 제작 도구에서 널리 쓰이는 교환 포맷
- `.obj`: 애니메이션이 없는 정적 피규어와 스캔 모델

`.blend`, `.unitypackage`, `.pmx`, `.pmd`는 직접 지원하지 않는다. 해당 파일은 원래 제작 도구에서 GLB 또는 VRM으로 변환한 뒤 가져오도록 안내한다.

결정할 내용:

- 임포트 가능한 최대 파일 크기와 폴리곤 수
- 외부 텍스처가 있는 GLTF의 경로 처리 방식
- 애니메이션이 여러 개일 때 클립과 정지 포즈를 선택하는 UI
- 모델 축, 단위, 원점이 제각각일 때 자동 보정 기준
- 손상되었거나 지원되지 않는 모델의 오류 안내

추천 기본 동작:

- 모델의 바운딩 박스를 계산해 적당한 크기로 자동 축소 또는 확대한다.
- 가장 낮은 지점을 선반 높이에 맞춘다.
- 애니메이션이 포함되어 있어도 기본값은 `정지`로 둔다.
- 사용자는 모델마다 애니메이션을 켜고 클립, 반복 여부와 재생 속도를 정한다.
- 애니메이션을 끄면 사용자가 선택한 시점 또는 포즈에서 고정한다.
- 애니메이션이 없는 모델에는 관련 설정을 표시하지 않는다.
- 원본 파일을 수정하지 않고 임포트 결과만 프로젝트에 기록한다.

### 6.2 2D 콘텐츠

2D 이미지는 화면에 붙는 UI나 빌보드가 아니라 진열장 안에 놓이는 실제 굿즈처럼 표현한다. 모든 타입은 3D 공간에서 두께를 가지며 조명, 카메라 원근, 선반 스냅과 그림자의 영향을 받는다.

2D 이미지는 다음 표현 중 하나로 전시할 수 있다.

- 평면 패널
- 투명 아크릴 스탠드
- 액자
- 포토 카드

#### 아크릴 스탠드

- 투명 PNG의 알파 외곽선을 따라 아크릴 커팅 라인을 생성한다.
- 이미지 바깥에 얇은 투명 여백을 두고 아크릴 판에 실제 두께를 준다.
- 판 모양은 이미지 외곽선, 사각형, 원형·타원 중 선택하며 외곽 여백은 슬라이더와 숫자 입력으로 조절한다.
- 이미지 인쇄면과 투명 아크릴 테두리를 구분해 광택과 반사가 보이게 한다.
- 투명 원형, 사각형 또는 색상 받침대를 선택할 수 있다.
- 받침대 홈에 캐릭터 판이 꽂힌 형태로 만들고 선반 위에 자동으로 세운다.
- 앞면만 인쇄, 양면 인쇄와 뒷면 처리 방식을 선택할 수 있다.

알파 외곽선이 지나치게 복잡하거나 투명 영역이 없는 이미지는 사각 아크릴 패널로 안전하게 대체할 수 있어야 한다.

#### 다른 전시 타입

- 평면 패널: 투명 또는 불투명 사각 아크릴 판에 이미지를 인쇄
- 액자: 프레임, 매트와 뒷판을 가진 벽걸이 또는 탁상형 전시물
- 포토 카드: 얇은 카드와 투명 보호 케이스 또는 카드 스탠드

#### 사용자 조절 항목

- 전시 타입
- 실제 크기와 두께
- 아크릴 외곽 여백
- 아크릴 투명도, 색상과 광택
- 받침대 모양, 크기와 색상
- 프레임 또는 카드 케이스 재질
- 앞뒤 인쇄 방식

v1에서는 `평면 패널`, `아크릴 스탠드`, `액자`, `포토 카드`를 제공한다. 투명 PNG와 WebP는 알파 채널을 유지하고 아크릴 스탠드를 기본값으로 제안한다. JPG처럼 알파 채널이 없는 이미지는 사각 아크릴 패널을 기본값으로 제안한다. 자동 배경 제거는 서버 비용이나 온디바이스 모델이 필요한 별도 기능이므로 v1 이후 확장 후보로 둔다.

### 6.3 모델 공급과 제품 경계

- 앱은 제3자 3D 모델을 기본 제공하거나 재배포하지 않는다.
- 사용자는 무료, 유료, 직접 제작 또는 커미션 모델을 외부에서 구해 로컬 파일로 가져온다.
- 앱은 모델 사이트를 검색하거나 계정, 결제, 다운로드를 연결하지 않는다.
- 외부 모델 출처 소개가 필요하면 앱이 아니라 공식 웹사이트와 사용 가이드에 링크를 제공한다.
- 온보딩에 시각적 예시가 필요하면 저작권이 있는 캐릭터 대신 코드로 생성한 단순 도형 또는 자체 제작 에셋만 사용한다.

### 6.4 저작권과 안전

- 사용자는 자신이 불러온 콘텐츠를 이용할 권한이 있는지 직접 확인한다.
- 사용자가 불러온 콘텐츠는 외부 서버에 업로드하지 않는다.
- VRM 파일에 포함된 작성자와 이용 조건 메타데이터는 제거하지 않고 모델 상세 정보에서 확인할 수 있게 한다.
- 일반 GLB, GLTF, FBX와 OBJ는 라이선스를 일관되게 판독할 수 없으므로 출처 URL과 라이선스 메모를 사용자가 선택적으로 기록할 수 있게 한다.
- 에셋이 포함된 `.uvd`는 개인 백업 및 본인 기기 간 이동 용도로 명확히 표시한다.
- 다른 사람과 공유하는 진열장 프리셋은 모델과 이미지 원본을 포함하지 않고 배치 정보와 빈 자리만 저장한다.
- 진열장 이미지를 내보낼 때에도 원본 모델의 이미지·영상 이용 조건은 사용자 책임임을 안내한다.

## 7. 프로젝트와 데이터 저장

### 추천 구조

진열장 하나를 하나의 프로젝트로 취급한다.

```text
MyCabinet.uvd/
├── project.json
├── assets/
│   ├── models/
│   └── images/
└── thumbnail.png
```

`project.json`에 저장할 주요 정보:

- 포맷 버전
- 진열장 템플릿과 크기
- 창 위치, 창 크기, 연결된 모니터
- 카메라 위치와 줌
- 조명 설정
- 오브젝트별 에셋 경로
- 오브젝트별 출처 URL과 라이선스 메모
- 오브젝트별 위치, 회전, 크기
- 2D 전시 타입과 모델별 애니메이션 활성화, 클립, 고정 시점, 반복과 속도 설정

결정할 내용:

- `.uvd`를 실제 폴더로 둘지 ZIP 기반 단일 파일로 만들지
- 자동 저장 시점과 백업 개수
- 에셋 중복 복사를 줄이기 위한 캐시 사용 여부
- 프로젝트 포맷이 바뀔 때 마이그레이션 방식

추천 방향:

- 개발 중에는 디버깅하기 쉬운 폴더 구조를 사용한다.
- 배포 단계에서는 개인 백업용 ZIP 기반 `.uvd` 단일 파일을 제공한다.
- 에셋 없는 배치 프리셋 내보내기는 `.uvd` 개인 백업과 명확히 구분한다.
- 자동 저장은 변경 후 짧은 지연을 두고 실행하며 최근 정상 상태를 별도로 백업한다.

## 8. 기술 방향 제안

### 확정 스택

- 데스크톱 셸: Electron
- UI: React + TypeScript
- 3D 렌더링: Three.js, 필요하면 React Three Fiber
- 포맷 로더: Three.js GLTFLoader, FBXLoader, OBJLoader와 `@pixiv/three-vrm`
- 상태 관리: 초기에는 가벼운 단일 스토어
- 네이티브 기능: Electron 메인 프로세스와 BrowserWindow API, 필요한 경우에만 OS별 네이티브 모듈
- 프로세스 보안: Context Isolation 활성화, Node Integration 비활성화, 제한된 Preload API 사용
- 로컬 설정: Electron 사용자 데이터 경로의 JSON 또는 SQLite
- 테스트: 단위 테스트 + 프로젝트 저장/불러오기 통합 테스트 + OS별 수동 창 동작 테스트

### 먼저 만들어야 할 기술 실험

본격적인 UI 개발 전에 내부 기술 스파이크로 다음을 검증한다. 이것은 별도 제품이나 공개 시험판이 아니라 v1 구현 과정의 첫 작업이다.

1. Windows와 macOS에서 투명 창이 안정적으로 렌더링되는가?
2. 클릭 통과를 실행 중에 켜고 끌 수 있는가?
3. 항상 위 옵션과 클릭 통과가 함께 정상 동작하는가?
4. 투명 창 안에서 WebGL의 알파, 그림자와 안티앨리어싱 품질이 충분한가?
5. 다중 모니터와 화면 배율 변경 뒤에도 위치가 복원되는가?
6. GLB, VRM, FBX, OBJ와 투명 PNG를 드래그해 즉시 표시할 수 있는가?
7. 분리형 GLTF, FBX와 OBJ의 외부 텍스처 및 재질 경로를 안정적으로 찾을 수 있는가?
8. VRM 0.x와 1.0의 MToon 재질, 휴머노이드와 라이선스 메타데이터를 올바르게 읽는가?

이 실험이 실패하면 제품 전체를 포기하는 대신 일반 창 또는 바탕화면 앱 형태로 UX를 조정할 수 있어야 한다.

### 성능 목표 초안

- 감상 모드 유휴 상태에서 CPU 사용을 최소화한다.
- 화면에 변화가 없을 때 렌더링 빈도를 낮추거나 렌더링을 멈춘다.
- 기본 진열장에서 일반 노트북 내장 GPU로 부드럽게 동작한다.
- 품질 프리셋을 `낮음 / 기본 / 높음`으로 제공한다.
- 비정상적으로 큰 모델을 불러올 때 경고하고 취소할 수 있게 한다.

Electron 자체의 메모리 사용량을 줄이기 위한 과도한 구조 변경은 하지 않는다. 실제 사용에 문제가 생기기 전까지는 개발 단순성과 안정성을 우선하고, Three.js 렌더 루프 일시 정지와 프레임 제한 같은 효과가 명확한 최적화만 기본 적용한다.

정확한 수치는 첫 기술 스파이크 측정 후 확정한다.

## 9. OS 지원 전략

동시 출시를 목표로 하더라도 투명 창, 클릭 통과, 바탕화면 계층, Dock/작업 표시줄 동작은 OS별로 별도 검증이 필요하다.

### 결정 기준

- 개발자가 일상적으로 테스트하기 편한 OS
- 잠재 사용자층의 OS 비중
- 투명 창과 클릭 통과 구현 안정성
- 설치 파일 서명과 배포 난이도

### 현재 추천

1. 공용 코드로 v1을 구현한다.
2. 개발 환경의 주 OS에서 일상적으로 검증한다.
3. Windows CI와 실제 Windows 기기 검증을 초기부터 병행한다.
4. 두 OS의 기능 차이는 숨기지 않고 명시적으로 처리하거나 문서화한다.
5. 배포 전에 실제 기기에서 설치, 권한, 절전, 다중 모니터와 업데이트를 테스트한다.

## 10. 배포 방식과 향후 수익화

v1은 itch.io에서 **무료로 다운로드하고 원하는 사용자가 후원할 수 있는 방식**으로 배포한다. 결제 여부에 따라 기능을 제한하거나 체험판을 따로 만들지 않는다.

- 앱 안에 결제, 구독 또는 후원 UI를 넣지 않는다.
- 후원은 itch.io의 외부 결제 흐름만 사용한다.
- v1의 소스 코드 및 사용자 콘텐츠 관련 라이선스 문구는 배포 준비 중 별도로 확정한다.
- 유료 테마나 고급 연출 같은 향후 수익화는 실제 이용 패턴을 확인한 뒤 별도 결정한다.
- 계정, 클라우드와 마켓플레이스를 수익화를 이유로 v1 범위에 추가하지 않는다.

## 11. 검증해야 할 핵심 가설

1. 사용자는 자신의 파일을 진열장에 배치하는 과정을 즐긴다.
2. 완성된 진열장을 한 번 보고 끝내지 않고 데스크톱에 계속 켜둔다.
3. 3D 파일이 없는 사용자도 2D 이미지만으로 충분히 즐길 수 있다.
4. 항상 위와 클릭 통과가 실제 작업을 방해하지 않는다.
5. 사용자는 사실적인 렌더링보다 쉬운 가져오기와 꾸미기를 더 중요하게 생각한다.

### 초기 사용자 테스트에서 물어볼 것

- 처음 파일을 넣고 배치하기까지 막힌 부분이 있었는가?
- 진열장을 평소에 어디에, 얼마나 오래 켜두었는가?
- 가장 먼저 바꾸고 싶었던 꾸미기 요소는 무엇인가?
- 다시 앱을 열게 만든 이유 또는 열지 않은 이유는 무엇인가?
- 자신의 진열장을 다른 사람에게 보여주고 싶었는가?

## 12. v1 구현 작업 계획

아래 작업 묶음은 별도로 공개하는 제품 단계가 아니다. 모든 작업과 출시 조건을 완료한 뒤 하나의 v1 정식 배포본으로 공개한다.

### 작업 1 — 데스크톱 기반과 렌더러

- Electron 앱과 React/Three.js 렌더링 구조
- 투명 전시 창과 일반 편집 창의 분리 구조
- 전시 창의 항상 위, 클릭 통과와 편집 입력 상태 전환
- 창 크기와 다중 모니터 위치 복원
- GLB/GLTF, VRM, FBX, OBJ 및 이미지 파일 드롭
- `@pixiv/three-vrm`을 통한 VRM 0.x와 1.0 로딩
- 렌더링 품질 설정과 유휴 상태 최적화
- macOS와 Windows 동작 차이를 감싸는 플랫폼 계층

완료 기준: 두 OS에서 투명 진열장을 장시간 실행하고 창 모드를 안전하게 전환할 수 있다.

### 작업 2 — 진열장 편집 경험

- 미술 방향을 공유하는 여러 진열장 프리셋과 선반 사용자 설정
- 프리셋 변경 시 기존 전시물 배치 유지
- 오브젝트 선택, 이동, 회전, 크기 조절
- 슬롯에 얽매이지 않는 자유 배치, 겹침과 쌓기
- 바닥·선반·오브젝트 표면 스냅, 복제, 잠금과 삭제
- 단순 충돌체 자동 생성, 기본 중력, 물리 안착과 자동 휴면
- 전시물별 넘어짐 방지, 충돌 무시와 배치 잠금
- Undo/Redo
- 기본 3/4 시점, 제한된 카메라 회전·줌과 조명 편집
- 감상 모드와 편집 모드 전환

완료 기준: 새 사용자가 별도 설명 없이 원하는 전시물을 배치하고 감상 모드로 전환할 수 있다.

### 작업 3 — 콘텐츠와 프로젝트

- 3D 모델 자동 크기 및 바닥 보정
- 모델별 애니메이션 기본 정지, 켜기/끄기, 클립, 고정 포즈, 반복과 속도 설정
- 2D 전시 타입 4종
- 여러 진열장 프로젝트 관리와 한 개의 활성 프로젝트 표시
- 가져온 사용자 파일을 프로젝트 저장소에 복사
- 자동 저장, 백업과 비정상 종료 복구
- 개인 백업용 `.uvd` 가져오기와 내보내기
- 사용자 에셋을 제외한 배치 프리셋 내보내기
- PNG 캡처

완료 기준: 다양한 정상·비정상 파일을 처리하고, 만든 진열장을 잃지 않으며 다른 기기로 옮길 수 있다.

### 작업 4 — 제품 완성도

- 첫 실행 온보딩
- 설정, 단축키와 한국어·영어·일본어·중국어 간체 UI
- 번역 문자열의 코드 분리와 언어별 리소스 관리
- 키보드 접근성과 UI 배율 대응
- 로컬 오류 메시지와 사용자가 직접 내보내는 진단 로그
- 계정, 로그인, 원격 분석과 원격 오류 수집이 없는지 검증
- 앱 아이콘, 자체 제작 진열장·UI 에셋과 라이선스 고지
- 단위, 통합 및 주요 사용자 흐름 테스트

완료 기준: 개발 도구 없이도 사용법과 오류 원인을 이해할 수 있고 대표 시나리오가 자동·수동 테스트를 통과한다.

### 작업 5 — 배포 엔지니어링과 출시

- 미서명 Windows 설치 패키지
- 미서명·미공증 macOS 설치 패키지
- OS 보안 경고와 수동 실행 절차를 설명하는 설치 안내
- itch 앱 패치 업데이트와 웹 직접 다운로드의 수동 업데이트 안내
- 새 설치, 업데이트, 제거 테스트
- 개인정보 처리방침, 사용 설명서와 릴리스 노트
- itch.io 프로젝트 페이지와 소개 이미지
- `butler`의 Windows 및 macOS 안정 채널을 이용한 빌드 업로드
- itch 앱 설치자의 패치 업데이트와 웹 직접 다운로드 사용자의 업데이트 경로 검증
- 릴리스 체크리스트와 알려진 문제 문서

완료 기준: 깨끗한 Windows/macOS 기기에서 안내 문서만 보고 미서명 패키지를 설치·실행할 수 있고, itch 앱 업데이트 또는 웹 재다운로드와 제거까지 문제없이 수행할 수 있다.

## 13. 제품 결정 현황

### 구현 전에 확정

| 결정 | 선택지 | 현재 추천 | 상태 |
|---|---|---|---|
| 주 개발 OS | macOS / Windows | macOS에서 개발하고 Windows 실제 기기와 CI로 병행 검증 | 미정 |
| 기본 미술 방향 | 사실적 / 귀여운 미니어처 / 미니멀 갤러리 / 혼합 | 전시물을 방해하지 않는 미니멀 갤러리에 따뜻한 미니어처 감성을 소량 혼합 | 확정 |
| 창 구조 | 한 창에서 편집·감상 전환 / 전시 창과 편집 창 분리 | 투명 전시 창과 일반 편집 패널을 분리 | 확정 |
| 동시 전시 수 | 한 개 / 여러 위젯 | 저장된 진열장은 여러 개, 화면에 활성화하는 진열장은 한 개 | 확정 |
| 카메라 | 고정 / 제한된 회전 / 완전 자유 | 기본 3/4 시점과 제한된 회전·줌으로 시작하고 사용성 테스트 후 조정 | 초기안 확정 |
| 파일 보관 | 원본 경로 참조 / 프로젝트에 복사 / 사용자 선택 | 프로젝트에 복사해 원본 이동·삭제의 영향을 받지 않게 함 | 확정 |
| 기본 창 동작 | 항상 위 / 일반 창 / 바탕화면 계층 | 일반 창을 기본으로 하고 항상 위와 클릭 통과는 사용자가 켬 | 확정 |
| 편집 도구 성격 | 정밀 3D 편집기 / 간단한 꾸미기 도구 | 자유 3D 배치에 스냅, 중력·충돌과 수치 편집을 보조로 제공 | 확정 |
| 충돌 처리 | 없음 / 요청형 안착 / 기본 중력·충돌 / 정밀 물리 | 단순 충돌체에 기본 중력을 적용하고 자동 휴면과 선택적 충돌 무시를 제공 | 확정 |
| 넘어짐 처리 | 항상 고정 / 물리대로 넘어짐 / 전시물별 설정 | `넘어짐 방지`를 전시물별로 제공하고 피규어의 기본값은 켬 | 확정 |

### 배포 전에 확정

| 결정 | 선택지 | 현재 추천 | 상태 |
|---|---|---|---|
| itch.io 가격 | 무료 / 원하는 만큼 지불 / 일회성 구매 | 무료 다운로드 + 선택적 후원 | 확정 |
| 직접 다운로드 업데이트 | 수동 재다운로드 / 앱 내 업데이트 확인 | itch 앱은 패치 업데이트, 웹 직접 설치는 itch.io에서 수동 재다운로드 | 확정 |
| 서명 | 미서명 / Windows 서명 / macOS 공증 포함 | 유료 코드 서명과 공증 없이 배포하고 설치·수동 실행 방법을 안내 | 확정 |
| 데이터 수집 | 없음 / 익명 오류 / 사용 분석 | 계정·로그인·원격 수집 없이 로컬 진단 로그만 제공 | 확정 |
| 언어 | 한국어 / 영어 / 일본어 / 중국어 | 한국어, 영어, 일본어와 중국어 간체 동시 지원 | 확정 |

현재 남은 큰 운영 결정은 주 개발 OS와 소스 코드 라이선스다. 홍보 방향, 스토어 확장과 장기 수익화는 제품이 실제로 동작한 뒤 결정해도 된다.

## 14. 현재 권장 기본안

현재 확정된 내용과 아직 임시인 항목을 합친 v1 기본안은 다음과 같다.

- macOS를 주 개발 환경으로 사용하고 Windows 검증을 병행해 동시 배포
- Electron + React + TypeScript + Three.js
- 테두리 없는 투명 전시 창과 별도의 일반 편집 창
- `항상 위`, `클릭 통과`를 개별 옵션으로 제공
- 미니멀 갤러리와 따뜻한 미니어처 감성을 공유하는 교체 가능한 여러 진열장 프리셋
- GLB/GLTF, VRM, FBX, OBJ 및 PNG/JPG 가져오기
- 애니메이션 기본 정지와 모델별 재생 설정, 실제 굿즈처럼 표현되는 2D 전시 타입 4종
- 겹침과 쌓기를 허용하는 자유 배치, 표면 스냅, 기본 중력·충돌과 자동 휴면
- 피규어별 넘어짐 방지, 충돌 무시와 완전한 배치 잠금
- 여러 진열장을 로컬에 저장하되 화면에는 활성화한 한 개만 표시
- 가져온 파일의 프로젝트 복사, 로컬 자동 저장과 개인 백업용 `.uvd`
- 한국어·영어·일본어·중국어 간체 UI, 온보딩, 설정과 로컬 진단 기능
- 계정, 로그인, 클라우드와 원격 데이터 수집이 없는 로컬 전용 구조
- 미서명 Windows/macOS 설치 파일, OS별 설치 안내와 itch 앱 패치 업데이트 지원
- itch.io 무료 다운로드와 선택적 후원
- itch.io를 첫 배포처로 사용하고 `butler`로 Windows/macOS 채널 관리
- 제3자 모델 기본 제공, 외부 모델 사이트 연결, 계정, 클라우드와 마켓플레이스는 제외

이 기본안의 목표는 **사용자가 자신의 파일 하나를 1분 안에 보기 좋은 전시물로 만들어 데스크톱에 계속 띄워두게 하는 것**이다.

---

## 결정 기록

실제로 결정을 내릴 때 아래 형식으로 추가한다.

```text
결정 날짜:
결정 항목:
선택:
선택 이유:
고려한 대안:
다시 검토할 조건:
```

### 2026-08-15 — 데스크톱 프레임워크와 3D 렌더러

- 선택: Electron + React + TypeScript + Three.js
- 선택 이유: 투명 위젯과 Three.js가 제품의 중심이므로 동일한 Chromium 환경, TypeScript 중심 개발과 Electron의 성숙한 창 제어 API를 우선한다.
- 고려한 대안: Tauri + Three.js
- 다시 검토할 조건: Electron 자체의 자원 사용량이 실제 사용자 환경에서 제품 사용을 방해하는 수준으로 측정될 때

### 2026-08-15 — 사용자 모델 공급과 외부 서비스 경계

- 선택: 앱은 제3자 모델을 제공하거나 모델 사이트와 연결하지 않고, 사용자가 가진 로컬 파일만 불러온다.
- 선택 이유: 무료·유료 모델은 외부 생태계에서 충분히 구할 수 있으며 앱은 진열 경험에 집중해야 한다.
- 고려한 대안: 앱 내부 무료 모델 목록, VRoid Hub·BOOTH 검색 및 다운로드 연동, 자체 마켓플레이스
- 다시 검토할 조건: 외부 모델을 구하고 가져오는 과정이 실제 사용자 테스트에서 반복적인 이탈 원인으로 확인될 때

### 2026-08-15 — 지원 포맷과 애니메이션 기본 동작

- 선택: v1에서 GLB, GLTF, VRM 0.x/1.0, FBX와 OBJ를 지원한다. 애니메이션은 모델별 설정이며 기본값은 정지다.
- 선택 이유: 일반 3D 모델과 일본 캐릭터형 VRM, 무료·유료 모델 마켓의 주요 교환 포맷을 함께 수용하면서 피규어다운 정적 전시를 기본 경험으로 유지한다.
- 고려한 대안: GLB/GLTF만 지원, 모든 애니메이션 자동 재생
- 다시 검토할 조건: 특정 포맷의 호환성 비용이 효용보다 크거나 사용자 파일 분포에서 거의 사용되지 않을 때

### 2026-08-15 — 2D 콘텐츠의 전시 방식

- 선택: 2D 이미지는 평면 UI가 아니라 두께와 재질을 가진 아크릴 스탠드, 패널, 액자 또는 포토 카드로 3D 진열한다.
- 선택 이유: 3D 피규어와 같은 공간에 놓였을 때 실제 수집품처럼 자연스럽게 보이게 하기 위해서다.
- 고려한 대안: 카메라를 바라보는 단순 스프라이트 또는 평면 이미지
- 다시 검토할 조건: 알파 외곽선 기반 아크릴 형상 생성이 특정 이미지에서 실패할 경우 사각 패널 대체 규칙을 개선한다.

### 2026-08-15 — 첫 배포처

- 선택: v1은 itch.io에서 Windows와 macOS용으로 배포한다.
- 선택 이유: 독립 데스크톱 앱을 빠르게 공개할 수 있고 `butler` 채널과 itch 앱을 통해 빌드 및 패치 업데이트를 관리할 수 있다.
- 고려한 대안: 자체 웹사이트 직접 배포, Microsoft Store, Mac App Store
- 다시 검토할 조건: 사용자층이 앱 스토어 설치 또는 별도의 공식 웹사이트 결제를 뚜렷하게 요구할 때

### 2026-08-15 — 진열장 미술 방향과 교체 가능한 프리셋

- 선택: 미니멀 갤러리에 따뜻한 미니어처 감성을 소량 섞은 방향을 공통 기반으로 삼고, 유리 진열장·프레임형·선반형 등 여러 프리셋을 교체할 수 있게 한다.
- 선택 이유: 서로 다른 스타일의 사용자 모델을 방해하지 않으면서 사용자가 진열장 분위기를 바꿀 수 있어야 하기 때문이다.
- 고려한 대안: 단일 진열장, 진열장마다 완전히 다른 강한 테마
- 다시 검토할 조건: 사용자 테스트에서 더 강한 장르별 테마 수요가 확인될 때

### 2026-08-15 — 전시 창, 편집 창과 프로젝트 활성 방식

- 선택: 투명 전시 창과 일반 편집 창을 분리한다. 여러 프로젝트를 저장하되 화면에는 활성화한 한 개만 표시한다.
- 선택 이유: 평상시에는 전시물만 깔끔하게 보이고, 편집할 때는 충분한 도구와 자유 배치 공간을 제공하기 위해서다.
- 고려한 대안: 한 창에서 감상·편집 전환, 여러 진열장 동시 표시
- 다시 검토할 조건: 여러 진열장을 동시에 띄우려는 요구가 성능과 사용성 비용보다 충분히 클 때

### 2026-08-15 — 자유 배치, 기본 중력과 넘어짐 방지

- 선택: 슬롯이나 격자로 배치를 제한하지 않고 단순 충돌체와 기본 중력으로 낙하와 쌓기를 처리한다. 피규어에는 물리 회전만 제한하는 `넘어짐 방지`를 기본 적용하며, 충돌 무시로 의도적인 겹침도 허용한다.
- 선택 이유: 공중에 뜨거나 바닥을 뚫는 배치를 자연스럽게 바로잡으면서도 캐릭터 피규어가 작은 충격에 계속 쓰러지지 않게 하기 위해서다.
- 고려한 대안: 충돌 없는 완전 자유 배치, 요청할 때만 안착 계산, 모든 전시물을 완전 고정, 메시 단위 정밀 물리 시뮬레이션
- 다시 검토할 조건: 단순 충돌체로 쌓기 품질이 부족하거나 넘어짐 방지가 사용자가 의도한 기울어진 배치를 방해할 때

### 2026-08-15 — 로컬 전용, 무료 후원형 배포와 다국어

- 선택: v1은 계정, 로그인, 클라우드와 원격 데이터 수집 없이 로컬에서 동작한다. itch.io에서 무료 다운로드와 선택적 후원 방식으로 미서명 Windows/macOS 패키지를 제공하며, 한국어·영어·일본어·중국어 간체를 지원한다.
- 선택 이유: 서버 운영과 유료 서명 비용 없이 핵심 진열 경험에 집중하고, 모델 생태계의 주요 사용자층이 바로 사용할 수 있게 하기 위해서다.
- 고려한 대안: 계정 기반 동기화, 유료 판매, 원격 오류 수집, 유료 코드 서명과 macOS 공증, 한국어·영어만 지원
- 다시 검토할 조건: 사용자 신뢰와 설치 전환율에 미서명 배포가 큰 장애가 되거나 번체 중국어 등 추가 언어 수요가 확인될 때
